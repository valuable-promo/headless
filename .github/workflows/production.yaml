name: Production

on:
  push:
    tags: [v*]

jobs:
  build:
    uses: powerkernel/actions-workflows/.github/workflows/build-production.yml@main
    with:
      username: ${{ github.actor }}
    secrets:
      PASSWORD: ${{ secrets.GITHUB_TOKEN }}
