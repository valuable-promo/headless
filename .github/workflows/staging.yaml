name: Staging

on:
  push:
    branches: [main]
    paths-ignore:
      - "**/**.md"
      - "docker-compose.yaml"
jobs:
  build:
    uses: valuable-promo/infra/.github/workflows/build-staging.yaml@main
    with:
      username: ${{ github.actor }}
    secrets:
      PASSWORD: ${{ secrets.GITHUB_TOKEN }}
